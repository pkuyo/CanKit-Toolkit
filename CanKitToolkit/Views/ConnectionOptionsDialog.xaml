<Window x:Class="CanKitToolkit.Views.ConnectionOptionsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="{DynamicResource Title_ConnOptions}" Height="260" Width="520"
        WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/CanKitToolkit;component/Resources/Converters.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Mode selection -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,8">
            <TextBlock Text="{DynamicResource Label_Protocol}" VerticalAlignment="Center" Margin="0,0,12,0"/>
            <RadioButton x:Name="Can20Radio" Content="{DynamicResource Option_Can20}" Margin="0,0,12,0"/>
            <RadioButton x:Name="CanFdRadio" Content="{DynamicResource Option_CanFd}"/>
        </StackPanel>

        <!-- Bitrates -->
        <Grid Grid.Row="1" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="160"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="160"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="{DynamicResource Label_BitRate}" Margin="0,0,8,0" VerticalAlignment="Center"/>
            <ComboBox x:Name="BitrateCombo" Grid.Column="1">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Converter={StaticResource BitrateConverter}, ConverterParameter=0.##}"/>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>

            <TextBlock x:Name="DataBitrateLabel" Grid.Column="3" Text="{DynamicResource Label_DataBitRate}" Margin="0,0,8,0" VerticalAlignment="Center"/>
            <ComboBox x:Name="DataBitrateCombo" Grid.Column="4">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Converter={StaticResource BitrateConverter}, ConverterParameter=0.##}"/>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </Grid>

        <!-- Advanced options + Filters -->
        <StackPanel Grid.Row="2">
            <!-- Advanced options row -->
            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                <CheckBox x:Name="ListenOnlyCheck" Content="{DynamicResource Option_ListenOnly}" Margin="0,0,16,0" Visibility="Collapsed"/>
                <TextBlock x:Name="ErrorCounterLabel" Text="{DynamicResource Label_ErrorCounterPeriod}" Margin="0,0,8,0" VerticalAlignment="Center" Visibility="Collapsed"/>
                <TextBox x:Name="ErrorCounterPeriodBox" Width="80" HorizontalAlignment="Left" TextAlignment="Right" Padding="4,2" Visibility="Collapsed"/>
                <TextBlock Text="ms" VerticalAlignment="Center" Margin="4,0,0,0" Visibility="Collapsed" x:Name="ErrorCounterUnit"/>
            </StackPanel>

            <!-- Filters row -->
            <StackPanel Orientation="Horizontal">
                <Button x:Name="EditFiltersBtn" Content="{DynamicResource Button_EditFilters}" Width="120" Click="OnEditFilters"/>
                <TextBlock Text="{DynamicResource Text_FilterApplyHint}" VerticalAlignment="Center" Foreground="Gray" Margin="15,0,0,0"/>
            </StackPanel>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="{DynamicResource Common_OK}" Width="80" Margin="0,0,8,0" IsDefault="True" Click="OnOk"/>
            <Button Content="{DynamicResource Common_Cancel}" Width="80" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
